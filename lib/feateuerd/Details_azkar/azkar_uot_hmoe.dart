import 'package:flutter/material.dart';

class AzkarSlwat extends StatelessWidget {
  const AzkarSlwat({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text('Uot home slwat'),
      ),
    );
  }
}