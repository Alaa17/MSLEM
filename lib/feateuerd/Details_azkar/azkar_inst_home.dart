import 'package:flutter/material.dart';

class AzkarTrivel extends StatelessWidget {
  const AzkarTrivel({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text('inst Home'),
      ),
    );
  }
}